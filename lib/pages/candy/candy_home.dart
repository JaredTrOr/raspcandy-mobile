import 'package:flutter/material.dart';

class CandyHome extends StatelessWidget {
  const CandyHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Candy home'),
    );
  }
}